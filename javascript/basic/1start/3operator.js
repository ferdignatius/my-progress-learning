// operator aritmatika
// +,-,*,/,%

// operator assignment
// +=, -=, *=, /=, %=
// x += y -> x = x + y

// operator pembanding
// ==, !=, <, >, <=, >=, ===
// == -> 'sama'
// === -> 'identik'

// operator logika
// &&, ||, !